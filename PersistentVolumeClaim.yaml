apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sqlite-db-pvc
spec:
  resources:
    requests:
      storage: 500Mi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  storageClassName: gp2-cheap
  
